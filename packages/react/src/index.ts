// Public API. The stylesheet is NOT imported here — consumers import it once
// explicitly: `import "@dept/react/styles.css"` (a copy of @dept/core's sheet).
export { Button, type ButtonProps } from "./components/Button";
